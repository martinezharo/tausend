// Everything is prerendered: the trainer is a static shell hydrated on the
// device, and the word pages are real HTML so they can be crawled.
export const prerender = true;
export const ssr = true;
